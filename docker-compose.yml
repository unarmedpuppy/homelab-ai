# Base docker-compose - shared networks and volumes
# Usage:
#   Server:     docker compose -f docker-compose.yml -f docker-compose.server.yml up -d
#   Gaming PC:  docker compose -f docker-compose.yml -f docker-compose.gaming.yml up -d

networks:
  ai-network:
    name: ai-network
    driver: bridge

volumes:
  huggingface-cache:
    name: huggingface-cache
  router-data:
    name: router-data
