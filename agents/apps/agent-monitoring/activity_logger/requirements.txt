# Activity Logger Requirements
# Minimal dependencies - only what's needed for logging

# For network-based logging (optional - falls back to SQLite if not available)
requests>=2.31.0

# Standard library dependencies (built-in, no install needed):
# - sqlite3
# - json
# - datetime
# - pathlib
# - os

