# Firefly III Environment Variables
# Copy this file to .env and fill in the values if needed
# Most configuration is done via docker-compose.yml environment variables

# APP_KEY will be generated on first run
# Run: docker exec -it firefly-iii php artisan key:generate --show
# Then update docker-compose.yml with the generated key

