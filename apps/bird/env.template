# Bird Bookmark Processor Configuration
# Copy to .env and fill in values

# Twitter/X Authentication (REQUIRED)
# Get these from your browser cookies after logging into X.com
# In browser DevTools > Application > Cookies > x.com
AUTH_TOKEN=
CT0=

# Local AI Router (default: Docker network address)
AI_ROUTER_URL=http://local-ai-router:8000

# Local AI Router API Key (REQUIRED for authentication)
# Create with: docker compose exec local-ai-router python scripts/manage-api-keys.py create "bird-bot"
AI_API_KEY=

# Git Configuration (for commits)
GIT_USER_NAME=Bird Bot
GIT_USER_EMAIL=bird@server.unarmedpuppy.com

# Processing Options
# How many bookmarks to fetch per run (default: 50)
BOOKMARK_LIMIT=50

# Run mode: "cron" (one-shot) or "daemon" (continuous with sleep interval)
RUN_MODE=cron

# Sleep interval in seconds if daemon mode (default: 6 hours)
SLEEP_INTERVAL=21600
