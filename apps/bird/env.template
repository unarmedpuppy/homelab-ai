# Bird Bookmark Storage Configuration
# Copy to .env and fill in values

# Twitter/X Authentication (REQUIRED)
# Get these from your browser cookies after logging into X.com
# In browser DevTools > Application > Cookies > x.com
AUTH_TOKEN=
CT0=

# Processing Options
# How many bookmarks to fetch per run (default: 50)
BOOKMARK_LIMIT=50

# Run mode: "cron" (one-shot) or "daemon" (continuous with sleep interval)
RUN_MODE=cron

# Sleep interval in seconds if daemon mode (default: 6 hours)
SLEEP_INTERVAL=21600
