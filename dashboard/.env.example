# API endpoint for the Local AI Router (memory, metrics endpoints - no auth needed)
VITE_API_URL=http://localhost:8012

# Mercury trading bot API
VITE_MERCURY_API_URL=https://mercury.server.unarmedpuppy.com

# API key for chat completions (injected by nginx proxy, not exposed to browser)
LOCAL_AI_API_KEY=lai_your_api_key_here
